# Launch Checklist

- Shopify app config created with embedded mode enabled
- Redirect URLs and scopes configured
- Production Postgres, Redis, and secrets provisioned
- Prisma migrations applied
- Webhooks registered and verified
- Billing plans created and tested
- Worker deployed with queue monitoring
- Structured logging and alerting configured
- Competitor module defaulted off
